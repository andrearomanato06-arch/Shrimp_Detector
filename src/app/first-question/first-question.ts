import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-first-question',
  imports: [RouterLink],
  templateUrl: './first-question.html',
  styleUrl: './first-question.css',
})
export class FirstQuestion {

}
